---
const title = 'アカウント | NUGBASE';
const description = 'NUGBASEのアカウント情報';
---

<!doctype html>
<html lang="ja">
	<head>
		<meta charset="UTF-8" />

		<meta
			name="viewport"
			content="width=device-width, initial-scale=1"
		/>

		<title>{title}</title>

		<meta
			name="description"
			content={description}
		/>

		<style>
			/* =========================================================
			   NUGBASE Account
			   ---------------------------------------------------------
			   NUGBASE Design System
			   - Minecraft-inspired block UI
			   - Light / Dark
			   - 8px grid
			   - Hard bottom shadow
			   - Grass-green accent
			   ========================================================= */


			/* =========================================================
			   01. Design Tokens
			   ========================================================= */

			:root {
				color-scheme: light;

				--account-background: #eef2f3;
				--account-surface: #ffffff;
				--account-surface-soft: #f5f7f7;

				--account-text: #202521;
				--account-text-soft: #59615c;
				--account-text-muted: #737b76;

				--account-border: #cfd6d2;

				--account-accent: #3f7d32;
				--account-accent-dark: #2f6327;
				--account-accent-soft: #e6f0e3;

				--account-danger: #a9473f;
				--account-danger-dark: #843832;
				--account-danger-soft: #f6e7e5;

				--account-shadow:
					0 8px 0 rgba(29, 40, 32, 0.12),
					0 16px 32px rgba(29, 40, 32, 0.10);

				--account-shadow-small:
					0 4px 0 rgba(29, 40, 32, 0.10),
					0 10px 20px rgba(29, 40, 32, 0.08);

				--account-radius: 8px;

				--account-max-width: 760px;
			}


			/* =========================================================
			   02. Dark Theme
			   ========================================================= */

			html[data-theme="dark"] {
				color-scheme: dark;

				--account-background: #111416;
				--account-surface: #292e32;
				--account-surface-soft: #33393d;

				--account-text: #f1f4f2;
				--account-text-soft: #c2cac5;
				--account-text-muted: #9ca59f;

				--account-border: #4b5357;

				--account-accent: #72ad55;
				--account-accent-dark: #5e9847;
				--account-accent-soft: #263829;

				--account-danger: #d66b61;
				--account-danger-dark: #b8544c;
				--account-danger-soft: #3b2827;

				--account-shadow:
					0 8px 0 rgba(0, 0, 0, 0.35),
					0 18px 36px rgba(0, 0, 0, 0.28);

				--account-shadow-small:
					0 4px 0 rgba(0, 0, 0, 0.30),
					0 10px 22px rgba(0, 0, 0, 0.22);
			}


			/* =========================================================
			   03. Base
			   ========================================================= */

			* {
				box-sizing: border-box;
			}

			html {
				min-height: 100%;
				background: var(--account-background);
			}

			body {
				margin: 0;
				min-height: 100vh;

				background:
					linear-gradient(
						180deg,
						rgba(86, 126, 73, 0.12) 0,
						transparent 280px
					),
					var(--account-background);

				color: var(--account-text);

				font-family:
					system-ui,
					-apple-system,
					BlinkMacSystemFont,
					"Segoe UI",
					"Yu Gothic",
					"YuGothic",
					"Hiragino Kaku Gothic ProN",
					Meiryo,
					sans-serif;

				line-height: 1.7;

				transition:
					background-color 0.2s ease,
					color 0.2s ease;
			}

			button,
			a {
				font: inherit;
			}

			button {
				color: inherit;
			}


			/* =========================================================
			   04. Page
			   ========================================================= */

			.account-page {
				width: min(
					calc(100% - 32px),
					var(--account-max-width)
				);

				margin: 0 auto;

				padding: 48px 0 64px;
			}


			/* =========================================================
			   05. Top Navigation
			   ========================================================= */

			.account-topbar {
				display: flex;
				align-items: center;
				justify-content: space-between;

				gap: 16px;

				margin-bottom: 24px;
			}

			.back-link {
				display: inline-flex;
				align-items: center;
				gap: 8px;

				min-height: 40px;

				padding: 8px 12px;

				border: 1px solid var(--account-border);

				background: var(--account-surface);

				color: var(--account-text-soft);

				text-decoration: none;

				box-shadow: 0 3px 0 rgba(29, 40, 32, 0.08);

				transition:
					background-color 0.15s ease,
					color 0.15s ease,
					transform 0.15s ease,
					box-shadow 0.15s ease;
			}

			.back-link:hover {
				background: var(--account-accent-soft);
				color: var(--account-accent-dark);

				transform: translateY(-1px);

				box-shadow:
					0 4px 0 rgba(29, 40, 32, 0.10);
			}

			.back-link:active {
				transform: translateY(2px);

				box-shadow:
					0 1px 0 rgba(29, 40, 32, 0.08);
			}

			.back-link-arrow {
				font-size: 1.1rem;
				line-height: 1;
			}


			/* =========================================================
			   06. Account Card
			   ========================================================= */

			.account-card {
				position: relative;

				overflow: hidden;

				border:
					1px solid
					var(--account-border);

				border-radius: var(--account-radius);

				background: var(--account-surface);

				box-shadow: var(--account-shadow);
			}

			.account-card::before {
				content: "";

				position: absolute;

				top: 0;
				left: 0;
				right: 0;

				height: 6px;

				background:
					linear-gradient(
						90deg,
						var(--account-accent),
						var(--account-accent-dark)
					);
			}

			.account-card-inner {
				padding: 40px;
			}


			/* =========================================================
			   07. Heading
			   ========================================================= */

			.account-heading {
				margin: 0 0 32px;
			}

			.account-heading-label {
				display: inline-flex;
				align-items: center;

				min-height: 28px;

				margin-bottom: 12px;

				padding: 4px 10px;

				border:
					1px solid
					color-mix(
						in srgb,
						var(--account-accent) 30%,
						var(--account-border)
					);

				background: var(--account-accent-soft);

				color: var(--account-accent-dark);

				font-size: 0.78rem;
				font-weight: 700;

				letter-spacing: 0.08em;
			}

			.account-heading h1 {
				margin: 0;

				color: var(--account-text);

				font-size: clamp(
					1.8rem,
					4vw,
					2.4rem
				);

				font-weight: 800;

				line-height: 1.25;

				letter-spacing: -0.02em;
			}

			.account-heading p {
				margin: 10px 0 0;

				color: var(--account-text-muted);

				font-size: 0.95rem;
			}


			/* =========================================================
			   08. Loading / Error
			   ========================================================= */

			.account-status {
				padding: 20px;

				border:
					1px solid
					var(--account-border);

				background: var(--account-surface-soft);

				color: var(--account-text-soft);

				text-align: center;
			}

			.account-error {
				padding: 18px 20px;

				border:
					1px solid
					var(--account-danger);

				border-left:
					5px solid
					var(--account-danger);

				background: var(--account-danger-soft);

				color: var(--account-danger-dark);

				line-height: 1.7;
			}


			/* =========================================================
			   09. Profile
			   ========================================================= */

			.account-profile {
				display: flex;
				align-items: center;

				gap: 20px;

				padding: 24px;

				border:
					1px solid
					var(--account-border);

				background: var(--account-surface-soft);

				box-shadow: var(--account-shadow-small);
			}

			.account-avatar {
				display: block;

				width: 80px;
				height: 80px;

				flex: 0 0 80px;

				border:
					3px solid
					var(--account-accent);

				border-radius: 8px;

				object-fit: cover;

				background:
					linear-gradient(
						135deg,
						var(--account-accent-soft),
						var(--account-surface)
					);

				box-shadow:
					0 4px 0
					color-mix(
						in srgb,
						var(--account-accent) 35%,
						transparent
					);
			}

			.account-info {
				min-width: 0;
			}

			.account-info h2 {
				margin: 0 0 4px;

				color: var(--account-text);

				font-size: 1.35rem;
				font-weight: 800;

				line-height: 1.4;

				overflow-wrap: anywhere;
			}

			.account-info p {
				margin: 0;

				color: var(--account-text-muted);

				font-size: 0.95rem;

				overflow-wrap: anywhere;
			}


			/* =========================================================
			   10. Account Details
			   ========================================================= */

			.account-details {
				margin-top: 24px;

				border:
					1px solid
					var(--account-border);

				background: var(--account-surface);
			}

			.account-detail {
				display: grid;

				grid-template-columns:
					minmax(100px, 0.35fr)
					minmax(0, 1fr);

				gap: 24px;

				padding: 16px 20px;

				border-bottom:
					1px solid
					var(--account-border);
			}

			.account-detail:last-child {
				border-bottom: 0;
			}

			.account-detail span {
				color: var(--account-text-muted);

				font-size: 0.88rem;
				font-weight: 600;
			}

			.account-detail strong {
				color: var(--account-text);

				font-size: 0.95rem;
				font-weight: 700;

				text-align: right;

				overflow-wrap: anywhere;
			}


			/* =========================================================
			   11. Wiki Editor
			   ========================================================= */

			.wiki-editor-card {
				margin-top: 24px;

				padding: 24px;

				border:
					1px solid
					var(--account-border);

				background: var(--account-surface-soft);

				box-shadow: var(--account-shadow-small);
			}

			.wiki-editor-heading {
				display: flex;
				align-items: flex-start;
				justify-content: space-between;

				gap: 16px;
			}

			.wiki-editor-heading-text {
				min-width: 0;
			}

			.wiki-editor-heading h2 {
				margin: 0;

				color: var(--account-text);

				font-size: 1.15rem;
				font-weight: 800;

				line-height: 1.4;
			}

			.wiki-editor-heading p {
				margin: 6px 0 0;

				color: var(--account-text-muted);

				font-size: 0.88rem;

				line-height: 1.7;
			}

			.wiki-editor-badge {
				display: inline-flex;
				align-items: center;
				justify-content: center;

				flex: 0 0 auto;

				min-height: 30px;

				padding: 4px 10px;

				border:
					1px solid
					color-mix(
						in srgb,
						var(--account-accent) 35%,
						var(--account-border)
					);

				background: var(--account-accent-soft);

				color: var(--account-accent-dark);

				font-size: 0.75rem;
				font-weight: 800;

				white-space: nowrap;
			}

			.wiki-editor-description {
				margin: 18px 0 0;

				color: var(--account-text-soft);

				font-size: 0.9rem;
			}


			/* =========================================================
			   12. Wiki Editor Buttons
			   ========================================================= */

			.wiki-editor-actions {
				display: grid;

				grid-template-columns:
					repeat(2, minmax(0, 1fr));

				gap: 12px;

				margin-top: 18px;
			}

			.wiki-editor-button {
				display: flex;
				align-items: center;
				justify-content: center;

				width: 100%;

				min-height: 48px;

				padding: 12px 18px;

				border:
					1px solid
					var(--account-accent-dark);

				border-radius: 6px;

				background: var(--account-accent);

				color: #ffffff;

				font-weight: 800;

				text-decoration: none;

				cursor: pointer;

				box-shadow:
					0 4px 0
					var(--account-accent-dark);

				transition:
					background-color 0.15s ease,
					transform 0.15s ease,
					box-shadow 0.15s ease,
					opacity 0.15s ease;
			}

			.wiki-editor-button:hover {
				background: var(--account-accent-dark);

				transform: translateY(-1px);

				box-shadow:
					0 5px 0
					color-mix(
						in srgb,
						var(--account-accent-dark) 90%,
						#000000
					);
			}

			.wiki-editor-button:active {
				transform: translateY(3px);

				box-shadow:
					0 1px 0
					var(--account-accent-dark);
			}

			.wiki-editor-button.secondary {
				border-color:
					var(--account-border);

				background:
					var(--account-surface);

				color:
					var(--account-text-soft);

				box-shadow:
					0 4px 0
					rgba(29, 40, 32, 0.10);
			}

			.wiki-editor-button.secondary:hover {
				background:
					var(--account-accent-soft);

				color:
					var(--account-accent-dark);

				box-shadow:
					0 5px 0
					rgba(29, 40, 32, 0.12);
			}

			.wiki-editor-button:disabled {
				opacity: 0.65;

				cursor: wait;

				transform: none;

				box-shadow:
					0 2px 0
					var(--account-accent-dark);
			}

			.wiki-editor-status {
				margin: 12px 0 0;

				color: var(--account-text-muted);

				font-size: 0.85rem;

				text-align: center;
			}

			.wiki-editor-status.error {
				color: var(--account-danger);
			}

			.wiki-editor-link {
				display: inline-flex;
				align-items: center;
				justify-content: center;

				width: 100%;

				min-height: 44px;

				margin-top: 18px;

				padding: 10px 16px;

				border:
					1px solid
					var(--account-border);

				border-radius: 6px;

				background: var(--account-surface);

				color: var(--account-text-soft);

				font-weight: 700;

				text-decoration: none;

				box-shadow:
					0 3px 0
					rgba(29, 40, 32, 0.08);

				transition:
					background-color 0.15s ease,
					color 0.15s ease,
					transform 0.15s ease,
					box-shadow 0.15s ease;
			}

			.wiki-editor-link:hover {
				background: var(--account-accent-soft);

				color: var(--account-accent-dark);

				transform: translateY(-1px);

				box-shadow:
					0 4px 0
					rgba(29, 40, 32, 0.10);
			}


			/* =========================================================
			   13. Admin
			   ========================================================= */

			.wiki-admin-section {
				margin-top: 24px;

				padding-top: 24px;

				border-top:
					1px solid
					var(--account-border);
			}

			.wiki-admin-label {
				margin: 0;

				color: var(--account-text-muted);

				font-size: 0.82rem;
				font-weight: 700;
			}

			.wiki-admin-button {
				display: flex;
				align-items: center;
				justify-content: center;

				width: 100%;

				min-height: 48px;

				margin-top: 12px;

				padding: 12px 18px;

				border:
					1px solid
					var(--account-border);

				border-radius: 6px;

				background:
					var(--account-surface);

				color:
					var(--account-text);

				font-weight: 800;

				text-decoration: none;

				box-shadow:
					0 4px 0
					rgba(29, 40, 32, 0.10);

				transition:
					background-color 0.15s ease,
					color 0.15s ease,
					transform 0.15s ease,
					box-shadow 0.15s ease;
			}

			.wiki-admin-button:hover {
				background:
					var(--account-accent-soft);

				color:
					var(--account-accent-dark);

				transform: translateY(-1px);

				box-shadow:
					0 5px 0
					rgba(29, 40, 32, 0.12);
			}

			.wiki-admin-button:active {
				transform: translateY(3px);

				box-shadow:
					0 1px 0
					rgba(29, 40, 32, 0.08);
			}


			/* =========================================================
			   14. Actions
			   ========================================================= */

			.account-actions {
				margin-top: 32px;

				padding-top: 24px;

				border-top:
					1px solid
					var(--account-border);
			}

			.account-actions-label {
				margin: 0 0 12px;

				color: var(--account-text-muted);

				font-size: 0.82rem;
				font-weight: 600;
			}

			.logout-button {
				display: flex;
				align-items: center;
				justify-content: center;

				width: 100%;

				min-height: 48px;

				padding: 12px 18px;

				border:
					1px solid
					var(--account-danger-dark);

				border-radius: 6px;

				background: var(--account-danger);

				color: #ffffff;

				font-weight: 800;

				cursor: pointer;

				box-shadow:
					0 4px 0
					var(--account-danger-dark);

				transition:
					background-color 0.15s ease,
					transform 0.15s ease,
					box-shadow 0.15s ease,
					opacity 0.15s ease;
			}

			.logout-button:hover {
				background: var(--account-danger-dark);

				transform: translateY(-1px);

				box-shadow:
					0 5px 0
					color-mix(
						in srgb,
						var(--account-danger-dark) 90%,
						#000000
					);
			}

			.logout-button:active {
				transform: translateY(3px);

				box-shadow:
					0 1px 0
					var(--account-danger-dark);
			}

			.logout-button:disabled {
				opacity: 0.65;

				cursor: wait;

				transform: none;

				box-shadow:
					0 2px 0
					var(--account-danger-dark);
			}

			.logout-status {
				margin: 12px 0 0;

				color: var(--account-danger);

				font-size: 0.9rem;

				text-align: center;
			}


			/* =========================================================
			   15. Focus
			   ========================================================= */

			a:focus-visible,
			button:focus-visible {
				outline:
					3px solid
					var(--account-accent);

				outline-offset: 3px;
			}


			/* =========================================================
			   16. Mobile
			   ========================================================= */

			@media (max-width: 600px) {
				.account-page {
					width: min(
						calc(100% - 20px),
						var(--account-max-width)
					);

					padding: 24px 0 40px;
				}

				.account-topbar {
					margin-bottom: 16px;
				}

				.back-link {
					min-height: 38px;

					padding: 7px 10px;

					font-size: 0.9rem;
				}

				.account-card-inner {
					padding: 28px 18px;
				}

				.account-heading {
					margin-bottom: 24px;
				}

				.account-heading h1 {
					font-size: 1.8rem;
				}

				.account-heading p {
					font-size: 0.9rem;
				}

				.account-profile {
					align-items: flex-start;

					gap: 16px;

					padding: 18px;
				}

				.account-avatar {
					width: 64px;
					height: 64px;

					flex-basis: 64px;
				}

				.account-info h2 {
					font-size: 1.15rem;
				}

				.account-info p {
					font-size: 0.85rem;
				}

				.account-details {
					margin-top: 20px;
				}

				.account-detail {
					display: flex;
					flex-direction: column;

					align-items: flex-start;

					gap: 4px;

					padding: 14px 16px;
				}

				.account-detail strong {
					text-align: left;
				}

				.wiki-editor-card {
					margin-top: 20px;

					padding: 18px;
				}

				.wiki-editor-heading {
					flex-direction: column;

					gap: 10px;
				}

				.wiki-editor-badge {
					align-self: flex-start;
				}

				.wiki-editor-actions {
					grid-template-columns: 1fr;
				}

				.wiki-admin-section {
					margin-top: 20px;

					padding-top: 20px;
				}

				.account-actions {
					margin-top: 24px;

					padding-top: 20px;
				}
			}


			/* =========================================================
			   17. Very Small Screens
			   ========================================================= */

			@media (max-width: 380px) {
				.account-page {
					width: min(
						calc(100% - 16px),
						var(--account-max-width)
					);
				}

				.account-card-inner {
					padding: 24px 14px;
				}

				.account-profile {
					padding: 14px;
				}

				.account-avatar {
					width: 56px;
					height: 56px;

					flex-basis: 56px;
				}

				.wiki-editor-card {
					padding: 14px;
				}
			}


			/* =========================================================
			   18. Reduced Motion
			   ========================================================= */

			@media (prefers-reduced-motion: reduce) {
				*,
				*::before,
				*::after {
					scroll-behavior: auto !important;

					transition-duration: 0.01ms !important;
					animation-duration: 0.01ms !important;
					animation-iteration-count: 1 !important;
				}
			}
		</style>
	</head>

	<body>
		<main class="account-page">

			<!-- =====================================================
			     Top Navigation
			     ===================================================== -->

			<div class="account-topbar">
				<a
					href="/"
					class="back-link"
				>
					<span
						class="back-link-arrow"
						aria-hidden="true"
					>
						←
					</span>

					<span>
						NUGBASEへ戻る
					</span>
				</a>
			</div>


			<!-- =====================================================
			     Account Card
			     ===================================================== -->

			<section
				class="account-card"
				aria-labelledby="account-title"
			>
				<div class="account-card-inner">

					<!-- Heading -->

					<header class="account-heading">
						<div class="account-heading-label">
							NUGBASE ACCOUNT
						</div>

						<h1 id="account-title">
							アカウント
						</h1>

						<p>
							NUGBASEのアカウント情報
						</p>
					</header>


					<!-- Loading / Error -->

					<div
						id="account-status"
						class="account-status"
						role="status"
						aria-live="polite"
					>
						読み込み中…
					</div>


					<!-- Account Content -->

					<div
						id="account-content"
						hidden
					>

						<!-- Profile -->

						<div class="account-profile">
							<img
								id="account-avatar"
								class="account-avatar"
								src=""
								alt=""
							/>

							<div class="account-info">
								<h2 id="account-name"></h2>

								<p id="account-email"></p>
							</div>
						</div>


						<!-- Details -->

						<div class="account-details">
							<div class="account-detail">
								<span>
									表示名
								</span>

								<strong
									id="account-display-name"
								></strong>
							</div>

							<div class="account-detail">
								<span>
									権限
								</span>

								<strong
									id="account-role"
								></strong>
							</div>
						</div>


						<!-- =================================================
						     Wiki Editor
						     ================================================= -->

						<section
							id="wiki-editor-card"
							class="wiki-editor-card"
							aria-labelledby="wiki-editor-title"
						>
							<div class="wiki-editor-heading">

								<div class="wiki-editor-heading-text">
									<h2 id="wiki-editor-title">
										Wiki編集
									</h2>

									<p id="wiki-editor-description">
										NUGBASE Wikiの記事を編集できます。
									</p>
								</div>

								<span
									id="wiki-editor-badge"
									class="wiki-editor-badge"
								>
									確認中
								</span>
							</div>


							<p
								id="wiki-editor-message"
								class="wiki-editor-description"
							>
								Wiki編集権限を確認しています。
							</p>


							<!-- General User -->

							<button
								id="wiki-editor-register-button"
								class="wiki-editor-button"
								type="button"
								hidden
							>
								Wiki編集者になる
							</button>


							<!-- Editor / Admin -->

							<div
								id="wiki-editor-actions"
								class="wiki-editor-actions"
								hidden
							>
								<a
									id="wiki-editor-edit-link"
									class="wiki-editor-button"
									href="/docs/"
								>
									Wikiを編集
								</a>

								<a
									id="wiki-editor-requests-link"
									class="wiki-editor-button secondary"
									href="/wiki/requests/"
								>
									自分の下書き・申請
								</a>
							</div>


							<!-- Wiki -->

							<a
								id="wiki-editor-link"
								class="wiki-editor-link"
								href="/docs/"
								hidden
							>
								Wikiを見る
							</a>


							<p
								id="wiki-editor-status"
								class="wiki-editor-status"
								role="status"
								aria-live="polite"
								hidden
							></p>


							<!-- Admin -->

							<div
								id="wiki-admin-section"
								class="wiki-admin-section"
								hidden
							>
								<p class="wiki-admin-label">
									管理者メニュー
								</p>

								<a
									class="wiki-admin-button"
									href="/admin/"
								>
									Wiki管理画面
								</a>
							</div>
						</section>


						<!-- Actions -->

						<div class="account-actions">
							<p class="account-actions-label">
								アカウント操作
							</p>

							<button
								id="logout-button"
								class="logout-button"
								type="button"
							>
								ログアウト
							</button>

							<p
								id="logout-status"
								class="logout-status"
								role="status"
								aria-live="polite"
								hidden
							></p>
						</div>

					</div>
				</div>
			</section>
		</main>


		<script>
			/* =========================================================
			   NUGBASE Theme
			   ========================================================= */

			const applyTheme = (): void => {
				const storedTheme =
					localStorage.getItem("nug-theme");

				if (
					storedTheme === "light" ||
					storedTheme === "dark"
				) {
					document.documentElement.dataset.theme =
						storedTheme;

					return;
				}

				const prefersDark =
					window.matchMedia(
						"(prefers-color-scheme: dark)",
					).matches;

				document.documentElement.dataset.theme =
					prefersDark
						? "dark"
						: "light";
			};

			applyTheme();


			/* =========================================================
			   Auth Types
			   ========================================================= */

			type AuthUser = {
				id?: string;
				display_name?: string;
				email?: string;
				avatar_url?: string;
				role?: string;
				expires_at?: string;
			};

			type AuthResponse = {
				ok?: boolean;
				loggedIn?: boolean;
				user?: AuthUser | null;
			};

			type EditorRegistrationResponse = {
				ok?: boolean;
				alreadyEditor?: boolean;
				error?: string;
				user?: AuthUser | null;
			};


			/* =========================================================
			   Elements
			   ========================================================= */

			const status =
				document.querySelector<HTMLElement>(
					"#account-status",
				);

			const content =
				document.querySelector<HTMLElement>(
					"#account-content",
				);

			const avatar =
				document.querySelector<HTMLImageElement>(
					"#account-avatar",
				);

			const name =
				document.querySelector<HTMLElement>(
					"#account-name",
				);

			const email =
				document.querySelector<HTMLElement>(
					"#account-email",
				);

			const displayName =
				document.querySelector<HTMLElement>(
					"#account-display-name",
				);

			const role =
				document.querySelector<HTMLElement>(
					"#account-role",
				);

			const logoutButton =
				document.querySelector<HTMLButtonElement>(
					"#logout-button",
				);

			const logoutStatus =
				document.querySelector<HTMLElement>(
					"#logout-status",
				);


			/* =========================================================
			   Wiki Editor Elements
			   ========================================================= */

			const wikiEditorBadge =
				document.querySelector<HTMLElement>(
					"#wiki-editor-badge",
				);

			const wikiEditorMessage =
				document.querySelector<HTMLElement>(
					"#wiki-editor-message",
				);

			const wikiEditorRegisterButton =
				document.querySelector<HTMLButtonElement>(
					"#wiki-editor-register-button",
				);

			const wikiEditorActions =
				document.querySelector<HTMLElement>(
					"#wiki-editor-actions",
				);

			const wikiEditorLink =
				document.querySelector<HTMLAnchorElement>(
					"#wiki-editor-link",
				);

			const wikiEditorStatus =
				document.querySelector<HTMLElement>(
					"#wiki-editor-status",
				);

			const wikiAdminSection =
				document.querySelector<HTMLElement>(
					"#wiki-admin-section",
				);


			/* =========================================================
			   Error
			   ========================================================= */

			const showError = (
				message: string,
			): void => {
				if (!status) return;

				status.className =
					"account-error";

				status.textContent =
					message;

				status.hidden = false;
			};


			/* =========================================================
			   Wiki Editor UI
			   ========================================================= */

			const updateWikiEditorUI = (
				user: AuthUser,
			): void => {
				const userRole =
					user.role || "user";


				/* -------------------------
				   Reset
				   ------------------------- */

				if (wikiEditorRegisterButton) {
					wikiEditorRegisterButton.hidden =
						true;
				}

				if (wikiEditorActions) {
					wikiEditorActions.hidden =
						true;
				}

				if (wikiEditorLink) {
					wikiEditorLink.hidden =
						false;
				}

				if (wikiAdminSection) {
					wikiAdminSection.hidden =
						true;
				}


				/* -------------------------
				   user
				   ------------------------- */

				if (userRole === "user") {
					if (wikiEditorBadge) {
						wikiEditorBadge.textContent =
							"一般ユーザー";
					}

					if (wikiEditorMessage) {
						wikiEditorMessage.textContent =
							"Wiki編集者になると、Wikiの記事を編集してレビュー申請できるようになります。";
					}

					if (wikiEditorRegisterButton) {
						wikiEditorRegisterButton.hidden =
							false;
					}

					return;
				}


				/* -------------------------
				   editor
				   ------------------------- */

				if (userRole === "editor") {
					if (wikiEditorBadge) {
						wikiEditorBadge.textContent =
							"Wiki編集者";
					}

					if (wikiEditorMessage) {
						wikiEditorMessage.textContent =
							"Wikiの記事を編集し、下書き保存やレビュー申請を行えます。";
					}

					if (wikiEditorActions) {
						wikiEditorActions.hidden =
							false;
					}

					return;
				}


				/* -------------------------
				   admin
				   ------------------------- */

				if (userRole === "admin") {
					if (wikiEditorBadge) {
						wikiEditorBadge.textContent =
							"管理者";
					}

					if (wikiEditorMessage) {
						wikiEditorMessage.textContent =
							"Wikiの編集・レビュー・公開を管理できます。";
					}

					if (wikiEditorActions) {
						wikiEditorActions.hidden =
							false;
					}

					if (wikiAdminSection) {
						wikiAdminSection.hidden =
							false;
					}

					return;
				}


				/* -------------------------
				   Unknown role
				   ------------------------- */

				if (wikiEditorBadge) {
					wikiEditorBadge.textContent =
						"権限未確認";
				}

				if (wikiEditorMessage) {
					wikiEditorMessage.textContent =
						"Wiki編集権限を確認できませんでした。";
				}

				if (wikiEditorActions) {
					wikiEditorActions.hidden =
						true;
				}

				if (wikiEditorLink) {
					wikiEditorLink.hidden =
						true;
				}
			};


			/* =========================================================
			   Register Wiki Editor
			   ========================================================= */

			const registerWikiEditor =
				async (): Promise<void> => {
					if (!wikiEditorRegisterButton) {
						return;
					}

					wikiEditorRegisterButton.disabled =
						true;

					wikiEditorRegisterButton.textContent =
						"登録中…";

					if (wikiEditorStatus) {
						wikiEditorStatus.hidden =
							false;

						wikiEditorStatus.className =
							"wiki-editor-status";

						wikiEditorStatus.textContent =
							"Wiki編集者として登録しています…";
					}

					try {
						const response =
							await fetch(
								"/api/wiki/editor",
								{
									method: "POST",

									credentials:
										"same-origin",

									headers: {
										Accept:
											"application/json",
									},
								},
							);

						const data =
							(await response.json()) as
								EditorRegistrationResponse;

						if (
							!response.ok ||
							!data.ok
						) {
							throw new Error(
								data.error ||
								`HTTP ${response.status}`,
							);
						}

						await loadAccount();

						if (wikiEditorStatus) {
							wikiEditorStatus.hidden =
								false;

							wikiEditorStatus.className =
								"wiki-editor-status";

							wikiEditorStatus.textContent =
								"Wiki編集者として登録されました。";
						}
					} catch (error) {
						console.error(
							"NUG Wiki Editor Registration Error:",
							error,
						);

						if (wikiEditorStatus) {
							wikiEditorStatus.hidden =
								false;

							wikiEditorStatus.className =
								"wiki-editor-status error";

							wikiEditorStatus.textContent =
								"Wiki編集者の登録に失敗しました。もう一度お試しください。";
						}

						wikiEditorRegisterButton.disabled =
							false;

						wikiEditorRegisterButton.textContent =
							"Wiki編集者になる";
					}
				};


			wikiEditorRegisterButton?.addEventListener(
				"click",
				() => {
					void registerWikiEditor();
				},
			);


			/* =========================================================
			   Load Account
			   ========================================================= */

			const loadAccount =
				async (): Promise<void> => {
					try {
						const response =
							await fetch(
								"/me",
								{
									method: "GET",

									credentials:
										"same-origin",

									headers: {
										Accept:
											"application/json",
									},
								},
							);

						if (!response.ok) {
							throw new Error(
								`HTTP ${response.status}`,
							);
						}

						const data =
							(await response.json()) as AuthResponse;

						if (
							!data.ok ||
							!data.loggedIn ||
							!data.user
						) {
							window.location.href =
								"/api/auth/google";

							return;
						}

						const user =
							data.user;

						const userName =
							user.display_name ||
							"アカウント";


						/* -------------------------
						   Name
						   ------------------------- */

						if (name) {
							name.textContent =
								userName;
						}


						/* -------------------------
						   Display Name
						   ------------------------- */

						if (displayName) {
							displayName.textContent =
								userName;
						}


						/* -------------------------
						   Email
						   ------------------------- */

						if (email) {
							email.textContent =
								user.email ||
								"";
						}


						/* -------------------------
						   Role
						   ------------------------- */

						if (role) {
							role.textContent =
								user.role ||
								"user";
						}


						/* -------------------------
						   Wiki Editor
						   ------------------------- */

						updateWikiEditorUI(
							user,
						);


						/* -------------------------
						   Avatar
						   ------------------------- */

						if (avatar) {
							if (
								user.avatar_url
							) {
								avatar.src =
									user.avatar_url;

								avatar.alt =
									`${userName}のプロフィール画像`;

								avatar.hidden =
									false;
							} else {
								avatar.hidden =
									true;
							}
						}


						/* -------------------------
						   Show Content
						   ------------------------- */

						if (status) {
							status.hidden =
								true;
						}

						if (content) {
							content.hidden =
								false;
						}
					} catch (error) {
						console.error(
							"NUG Account Error:",
							error,
						);

						showError(
							"アカウント情報を読み込めませんでした。",
						);
					}
				};


			/* =========================================================
			   Logout
			   ========================================================= */

			logoutButton?.addEventListener(
				"click",
				async () => {
					if (!logoutButton) {
						return;
					}

					logoutButton.disabled =
						true;

					logoutButton.textContent =
						"ログアウト中…";

					if (logoutStatus) {
						logoutStatus.hidden =
							true;

						logoutStatus.textContent =
							"";
					}

					try {
						const response =
							await fetch(
								"/api/auth/logout",
								{
									method: "POST",

									credentials:
										"same-origin",

									headers: {
										Accept:
											"application/json",
									},
								},
							);

						if (!response.ok) {
							throw new Error(
								`HTTP ${response.status}`,
							);
						}

						const data =
							(await response.json()) as {
								ok?: boolean;
							};

						if (!data.ok) {
							throw new Error(
								"Logout was not successful.",
							);
						}

						window.location.href =
							"/";
					} catch (error) {
						console.error(
							"NUG Logout Error:",
							error,
						);

						if (logoutStatus) {
							logoutStatus.textContent =
								"ログアウトに失敗しました。もう一度お試しください。";

							logoutStatus.hidden =
								false;
						}

						logoutButton.disabled =
							false;

						logoutButton.textContent =
							"ログアウト";
					}
				},
			);


			/* =========================================================
			   Initialize
			   ========================================================= */

			void loadAccount();
		</script>
	</body>
</html>