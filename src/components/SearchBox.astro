<section
	class="search-section"
>

	<button
		type="button"
		class="search-box"
		aria-label="NUGを検索"
	>

		<!-- ========================================
		     Directional Light
		======================================== -->

		<div
			class="search-light"
			aria-hidden="true"
		></div>


		<!-- ========================================
		     Search Icon
		======================================== -->

		<div class="search-icon">

			<svg
				viewBox="0 0 24 24"
				aria-hidden="true"
			>

				<circle
					cx="11"
					cy="11"
					r="6.5"
				></circle>

				<path
					d="M16 16L21 21"
				></path>

			</svg>

		</div>


		<!-- ========================================
		     Search Content
		======================================== -->

		<div class="search-content">

			<p class="search-label">
				SEARCH
			</p>

			<p class="search-title">
				NUGを検索
			</p>

			<p class="search-description">
				攻略・プラグイン・コマンドなどを検索できます
			</p>

		</div>


		<!-- ========================================
		     Keyboard Hint
		======================================== -->

		<span class="search-key">
			⌕
		</span>


		<!-- ========================================
		     Arrow
		======================================== -->

		<span class="search-arrow">
			→
		</span>

	</button>

</section>


<script>

	const searchBox =
		document.querySelector<HTMLButtonElement>(
			".search-box"
		);


	searchBox?.addEventListener(
		"click",
		() => {

			document.dispatchEvent(
				new CustomEvent(
					"nug:open-search"
				)
			);

		}
	);

</script>


<style>

/* ========================================
   Search Section
======================================== */

.search-section {

	position:
		relative;

	padding-top:
		40px;

	padding-bottom:
		80px;

}


/* ========================================
   Search Box
======================================== */

.search-box {

	position:
		relative;

	width:
		100%;

	display:
		flex;

	align-items:
		center;

	gap:
		18px;

	min-height:
		88px;

	padding:
		16px 20px;

	overflow:
		hidden;

	border:
		1px solid var(--color-border-strong);

	border-radius:
		0;

	background:
		var(--color-surface);

	color:
		var(--color-text);

	text-align:
		left;

	cursor:
		pointer;

	box-shadow:

		0 4px 0
		var(--color-border-strong),

		0 12px 28px
		var(--color-shadow);

	transition:

		transform var(--transition-normal),

		background-color var(--transition-normal),

		border-color var(--transition-normal),

		box-shadow var(--transition-normal);

}


/* ========================================
   Directional Light
======================================== */

.search-light {

	position:
		absolute;

	top:
		-150px;

	left:
		-120px;

	width:
		360px;

	height:
		300px;

	background:

		radial-gradient(
			ellipse,
			var(--light-highlight),
			transparent 68%
		);

	opacity:
		0.65;

	pointer-events:
		none;

	transition:
		opacity var(--transition-normal);

}


.search-box:hover .search-light {

	opacity:
		1;

}


/* ========================================
   Hover
======================================== */

.search-box:hover {

	transform:
		translateY(-3px);

	border-color:
		var(--color-accent);

	background:
		var(--color-surface-hover);

	box-shadow:

		0 7px 0
		var(--color-accent),

		0 18px 34px
		var(--color-shadow-strong);

}


/* ========================================
   Active
======================================== */

.search-box:active {

	transform:
		translateY(1px);

	box-shadow:

		0 2px 0
		var(--color-accent),

		0 7px 16px
		var(--color-shadow);

}


/* ========================================
   Search Icon Slot
======================================== */

.search-icon {

	position:
		relative;

	z-index:
		2;

	display:
		flex;

	align-items:
		center;

	justify-content:
		center;

	width:
		52px;

	height:
		52px;

	flex-shrink:
		0;

	border:
		1px solid var(--color-border-strong);

	border-radius:
		0;

	background:
		var(--color-surface-strong);

	box-shadow:

		inset 0 2px 0
		rgba(255, 255, 255, 0.30),

		inset 0 -2px 0
		rgba(0, 0, 0, 0.08),

		0 3px 0
		var(--color-border-strong);

	transition:

		background-color var(--transition-normal),

		box-shadow var(--transition-normal);

}


.search-box:hover .search-icon {

	background:
		var(--color-accent-soft);

	box-shadow:

		inset 0 2px 0
		rgba(255, 255, 255, 0.38),

		0 4px 0
		var(--color-accent);

}


/* ========================================
   SVG
======================================== */

.search-icon svg {

	width:
		22px;

	height:
		22px;

	fill:
		none;

	stroke:
		var(--color-text);

	stroke-width:
		1.8;

	stroke-linecap:
		round;

	stroke-linejoin:
		round;

}


/* ========================================
   Search Content
======================================== */

.search-content {

	position:
		relative;

	z-index:
		2;

	flex:
		1;

	min-width:
		0;

}


.search-label {

	margin-bottom:
		2px;

	font-size:
		9px;

	font-weight:
		850;

	letter-spacing:
		0.17em;

	color:
		var(--color-accent);

}


.search-title {

	font-size:
		17px;

	font-weight:
		800;

	line-height:
		1.3;

}


.search-description {

	margin-top:
		3px;

	font-size:
		12px;

	line-height:
		1.5;

	color:
		var(--color-text-secondary);

}


/* ========================================
   Search Key
======================================== */

.search-key {

	position:
		relative;

	z-index:
		2;

	display:
		flex;

	align-items:
		center;

	justify-content:
		center;

	width:
		34px;

	height:
		34px;

	flex-shrink:
		0;

	border:
		1px solid var(--color-border);

	background:
		var(--color-surface-subtle);

	box-shadow:
		0 2px 0 var(--color-border-strong);

	color:
		var(--color-text-secondary);

	font-size:
		17px;

}


/* ========================================
   Arrow
======================================== */

.search-arrow {

	position:
		relative;

	z-index:
		2;

	display:
		flex;

	align-items:
		center;

	justify-content:
		center;

	width:
		36px;

	height:
		36px;

	flex-shrink:
		0;

	border:
		1px solid var(--color-border);

	background:
		var(--color-surface-subtle);

	color:
		var(--color-text-secondary);

	font-size:
		18px;

	box-shadow:
		0 2px 0 var(--color-border-strong);

	transition:

		transform var(--transition-fast),

		background-color var(--transition-fast),

		color var(--transition-fast),

		box-shadow var(--transition-fast);

}


.search-box:hover .search-arrow {

	transform:
		translateX(5px);

	background:
		var(--color-accent);

	color:
		#ffffff;

	box-shadow:
		0 3px 0 rgba(0, 0, 0, 0.20);

}


/* ========================================
   Dark Theme
======================================== */

:global(html[data-theme="dark"]) .search-box {

	box-shadow:

		0 4px 0 rgba(0, 0, 0, 0.40),

		0 14px 30px rgba(0, 0, 0, 0.28);

}


:global(html[data-theme="dark"]) .search-light {

	opacity:
		0.30;

}


:global(html[data-theme="dark"]) .search-icon {

	box-shadow:

		inset 0 2px 0
		rgba(255, 255, 255, 0.08),

		inset 0 -2px 0
		rgba(0, 0, 0, 0.25),

		0 3px 0
		rgba(0, 0, 0, 0.40);

}


:global(html[data-theme="dark"]) .search-box:hover {

	box-shadow:

		0 7px 0 var(--color-accent),

		0 20px 38px rgba(0, 0, 0, 0.42);

}


/* ========================================
   Mobile
======================================== */

@media (max-width: 768px) {

	.search-section {

		padding-top:
			32px;

		padding-bottom:
			60px;

	}


	.search-box {

		min-height:
			78px;

		gap:
			12px;

		padding:
			14px;

	}


	.search-icon {

		width:
			46px;

		height:
			46px;

	}


	.search-icon svg {

		width:
			20px;

		height:
			20px;

	}


	.search-title {

		font-size:
			15px;

	}


	.search-description {

		font-size:
			11px;

	}


	.search-key {

		display:
			none;

	}


	.search-arrow {

		width:
			32px;

		height:
			32px;

		font-size:
			16px;

	}

}


/* ========================================
   Small Mobile
======================================== */

@media (max-width: 480px) {

	.search-box {

		min-height:
			72px;

		padding:
			12px;

	}


	.search-icon {

		width:
			42px;

		height:
			42px;

	}


	.search-label {

		font-size:
			8px;

	}


	.search-title {

		font-size:
			14px;

	}


	.search-description {

		font-size:
			10px;

	}

}

</style>