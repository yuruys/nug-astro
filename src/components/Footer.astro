<footer class="site-footer">

	<div class="footer-light"></div>

	<div class="footer-inner">

		<!-- ========================================
		     Brand
		======================================== -->

		<div class="footer-brand">

			<a
				href="/"
				class="footer-logo"
				aria-label="NUG ホーム"
			>
				NUG
			</a>

			<p class="footer-description">
				Nekozouneko Unofficial Guide
			</p>

			<p class="footer-note">
				非公式の攻略・情報ガイドサイトです。
			</p>

		</div>


		<!-- ========================================
		     Navigation
		======================================== -->

		<nav
			class="footer-links"
			aria-label="フッターナビゲーション"
		>

			<a href="/guide/">
				<span>攻略</span>
				<span class="footer-arrow">→</span>
			</a>

			<a href="/docs/">
				<span>Wiki</span>
				<span class="footer-arrow">→</span>
			</a>

			<a href="/news/">
				<span>ニュース</span>
				<span class="footer-arrow">→</span>
			</a>

			<a href="/tool/">
				<span>ツール</span>
				<span class="footer-arrow">→</span>
			</a>

			<a href="/about/">
				<span>NUGについて</span>
				<span class="footer-arrow">→</span>
			</a>

		</nav>

	</div>


	<!-- ========================================
	     Bottom
	======================================== -->

	<div class="footer-bottom">

		<div class="footer-bottom-inner">

			<p>
				NUG is an unofficial guide site.
			</p>

			<span class="footer-version">
				NUGBASE
			</span>

		</div>

	</div>

</footer>


<style>

/* =========================================================
   NUGBASE Footer
   ---------------------------------------------------------
   Visual direction
   → Minecraft / BSL-inspired block UI
   → Hard borders
   → Hard shadows
   → Gold / grass accents
   → No rounded UI
   ========================================================= */


/* =========================================================
   00. Global Footer Radius Lock
   ---------------------------------------------------------
   Footer UI must NEVER use rounded corners.
   ========================================================= */

.site-footer,
.site-footer *,
.site-footer *::before,
.site-footer *::after {

	border-radius:
		0 !important;

}


/* =========================================================
   01. Footer
   ========================================================= */

.site-footer {

	position:
		relative;

	margin-top:
		96px;

	background:
		var(--color-background-deep);

	border-top:
		2px solid var(--color-border-strong);

	box-shadow:
		inset 0 3px 0 var(--color-shadow);

	isolation:
		isolate;

	overflow:
		hidden;

}


/* =========================================================
   02. Top Light
   ---------------------------------------------------------
   Minecraft / BSL-like directional light.
   ========================================================= */

.footer-light {

	position:
		absolute;

	top:
		0;

	left:
		0;

	width:
		52%;

	height:
		2px;

	background:
		linear-gradient(
			90deg,
			var(--color-gold-light),
			var(--color-grass-light),
			transparent
		);

	opacity:
		0.9;

	pointer-events:
		none;

}


/* =========================================================
   03. Inner
   ========================================================= */

.footer-inner {

	position:
		relative;

	width:
		min(
			var(--content-width),
			calc(100% - 48px)
		);

	margin:
		0 auto;

	padding:
		56px 0 48px;

	display:
		grid;

	grid-template-columns:
		minmax(220px, 1fr)
		minmax(360px, 1.4fr);

	align-items:
		start;

	gap:
		64px;

}


/* =========================================================
   04. Brand
   ========================================================= */

.footer-brand {

	min-width:
		0;

}


/* =========================================================
   Logo
   ========================================================= */

.footer-logo {

	position:
		relative;

	display:
		inline-block;

	padding:
		2px 0;

	font-family:
		var(--font-display);

	font-size:
		30px;

	font-weight:
		900;

	letter-spacing:
		-0.07em;

	color:
		var(--color-text);

	text-shadow:
		2px 2px 0 var(--color-shadow);

	transition:
		transform var(--transition-fast),
		color var(--transition-fast),
		text-shadow var(--transition-fast);

}


/*
 * Small gold block under the logo.
 */

.footer-logo::after {

	content:
		"";

	position:
		absolute;

	left:
		0;

	bottom:
		-5px;

	width:
		24px;

	height:
		2px;

	background:
		var(--color-accent);

	box-shadow:
		8px 0 0 var(--color-grass-light);

	transition:
		width var(--transition-fast),
		box-shadow var(--transition-fast);

}


.footer-logo:hover {

	color:
		var(--color-accent);

	transform:
		translate(-1px, -1px);

	text-shadow:
		3px 3px 0 var(--color-shadow);

}


.footer-logo:hover::after {

	width:
		32px;

	box-shadow:
		10px 0 0 var(--color-grass-light);

}


.footer-logo:active {

	transform:
		translate(1px, 2px);

}


/* =========================================================
   Brand Description
   ========================================================= */

.footer-description {

	margin-top:
		10px;

	font-size:
		12px;

	font-weight:
		700;

	letter-spacing:
		0.02em;

	color:
		var(--color-text-secondary);

}


.footer-note {

	max-width:
		300px;

	margin-top:
		14px;

	font-size:
		12px;

	line-height:
		1.7;

	color:
		var(--color-text-tertiary);

}


/* =========================================================
   05. Navigation
   ========================================================= */

.footer-links {

	display:
		grid;

	grid-template-columns:
		repeat(3, minmax(0, 1fr));

	gap:
		8px;

}


/* =========================================================
   Navigation Item
   ========================================================= */

.footer-links a {

	position:
		relative;

	min-height:
		50px;

	padding:
		0 14px;

	display:
		flex;

	align-items:
		center;

	justify-content:
		space-between;

	gap:
		12px;

	border:
		1px solid var(--color-border-strong);

	background:
		var(--color-surface);

	color:
		var(--color-text-secondary);

	box-shadow:
		0 3px 0 var(--color-border-strong);

	font-size:
		12px;

	font-weight:
		700;

	overflow:
		hidden;

	transition:
		background var(--transition-fast),
		color var(--transition-fast),
		border-color var(--transition-fast),
		box-shadow var(--transition-fast),
		transform var(--transition-fast);

}


/* =========================================================
   Top Gold / Grass Highlight
   ========================================================= */

.footer-links a::before {

	content:
		"";

	position:
		absolute;

	top:
		0;

	left:
		0;

	right:
		0;

	height:
		3px;

	background:
		linear-gradient(
			90deg,
			var(--color-gold-light),
			var(--color-grass-light)
		);

	opacity:
		0;

	transform:
		scaleX(0);

	transform-origin:
		left center;

	transition:
		opacity var(--transition-fast),
		transform var(--transition-fast);

}


/* =========================================================
   Left Block Accent
   ========================================================= */

.footer-links a::after {

	content:
		"";

	position:
		absolute;

	left:
		0;

	top:
		3px;

	bottom:
		0;

	width:
		2px;

	background:
		var(--color-accent);

	opacity:
		0;

	transition:
		opacity var(--transition-fast);

}


/* =========================================================
   Hover
   ========================================================= */

.footer-links a:hover {

	background:
		var(--color-surface-hover);

	color:
		var(--color-text);

	border-color:
		var(--color-accent);

	transform:
		translate(-1px, -2px);

	box-shadow:
		3px 5px 0 var(--color-shadow);

}


.footer-links a:hover::before {

	opacity:
		1;

	transform:
		scaleX(1);

}


.footer-links a:hover::after {

	opacity:
		1;

}


/* =========================================================
   Active / Pressed
   ========================================================= */

.footer-links a:active {

	transform:
		translate(1px, 2px);

	box-shadow:
		0 1px 0 var(--color-border);

}


/* =========================================================
   Arrow
   ========================================================= */

.footer-arrow {

	flex-shrink:
		0;

	color:
		var(--color-text-tertiary);

	font-family:
		var(--font-mono);

	font-size:
		13px;

	font-weight:
		800;

	transition:
		transform var(--transition-fast),
		color var(--transition-fast);

}


.footer-links a:hover .footer-arrow {

	transform:
		translateX(4px);

	color:
		var(--color-accent);

}


/* =========================================================
   06. Bottom
   ========================================================= */

.footer-bottom {

	border-top:
		1px solid var(--color-border-strong);

	background:
		var(--color-background);

	box-shadow:
		inset 0 1px 0 var(--color-shadow);

}


/* =========================================================
   Bottom Inner
   ========================================================= */

.footer-bottom-inner {

	width:
		min(
			var(--content-width),
			calc(100% - 48px)
		);

	margin:
		0 auto;

	min-height:
		58px;

	display:
		flex;

	align-items:
		center;

	justify-content:
		space-between;

	gap:
		20px;

	font-size:
		10px;

	color:
		var(--color-text-tertiary);

}


/* =========================================================
   Copyright Text
   ========================================================= */

.footer-bottom-inner p {

	color:
		var(--color-text-tertiary);

}


/* =========================================================
   NUGBASE Label
   ========================================================= */

.footer-version {

	position:
		relative;

	display:
		inline-flex;

	align-items:
		center;

	min-height:
		28px;

	padding:
		0 10px;

	border:
		1px solid var(--color-border-strong);

	background:
		var(--color-surface-subtle);

	box-shadow:
		2px 2px 0 var(--color-border);

	font-size:
		9px;

	font-weight:
		800;

	letter-spacing:
		0.08em;

	color:
		var(--color-text-secondary);

}


/*
 * Gold top edge.
 */

.footer-version::before {

	content:
		"";

	position:
		absolute;

	top:
		-1px;

	left:
		-1px;

	width:
		14px;

	height:
		2px;

	background:
		var(--color-accent);

}


/* =========================================================
   07. Tablet / Mobile
   ========================================================= */

@media (max-width: 768px) {

	.site-footer {

		margin-top:
			72px;

	}


	.footer-inner {

		width:
			calc(100% - 32px);

		padding:
			44px 0 36px;

		grid-template-columns:
			1fr;

		gap:
			32px;

	}


	.footer-links {

		grid-template-columns:
			repeat(2, minmax(0, 1fr));

	}


	.footer-links a {

		min-height:
			50px;

	}


	.footer-bottom-inner {

		width:
			calc(100% - 32px);

		min-height:
			56px;

	}

}


/* =========================================================
   08. Small Mobile
   ========================================================= */

@media (max-width: 480px) {

	.footer-inner {

		width:
			calc(100% - 24px);

	}


	.footer-links {

		grid-template-columns:
			1fr;

	}


	.footer-bottom-inner {

		width:
			calc(100% - 24px);

		padding:
			12px 0;

		min-height:
			auto;

		flex-direction:
			column;

		align-items:
			flex-start;

		gap:
			10px;

	}

}


/* =========================================================
   09. Keyboard Focus
   ========================================================= */

.footer-links a:focus-visible,
.footer-logo:focus-visible {

	outline:
		2px solid var(--color-accent);

	outline-offset:
		3px;

}


/* =========================================================
   10. Reduced Motion
   ========================================================= */

@media (prefers-reduced-motion: reduce) {

	.footer-logo,
	.footer-logo::after,
	.footer-links a,
	.footer-links a::before,
	.footer-links a::after,
	.footer-arrow {

		transition:
			none;

	}

}

</style>