<section class="hero">
  <!-- Background -->
  <div class="hero-background" aria-hidden="true">
    <div class="hero-image"></div>
    <div class="hero-noise"></div>

    <div class="hero-particles">
      <span></span><span></span><span></span>
      <span></span><span></span><span></span>
    </div>
  </div>

  <!-- Content -->
  <div class="hero-content">
    <p class="hero-description">
      <strong>
        NUGは、Nekozounekoの攻略情報をまとめた非公式ファンサイトです。
      </strong>
      <br />
      サーバー別の攻略情報や、プラグインの仕様をまとめたWiki、便利なツールなどを掲載しています。
      <br class="desktop-break" />
      本サイトは非公式です。情報は実際のプレイをもとに検証していますが、
      誤りがあればお知らせいただけると助かります。
    </p>
  </div>
</section>

<style>
/* =========================================================
   Hero Banner（縦幅縮小）
========================================================= */
.hero {
  position: relative;
  width: 100vw;
  height: 180px; /* ← 260px → 180px に縮小 */
  margin-left: calc(50% - 50vw);
  display: flex;
  align-items: center;
  overflow: hidden;
}

/* =========================================================
   Background（単色）
========================================================= */
.hero-background {
  position: absolute;
  inset: 0;
  overflow: hidden;
  pointer-events: none;
  background: #EEF2F3;
}

.hero-image {
  position: absolute;
  inset: -8px; /* ← 縦幅縮小に合わせて調整 */
  background: url("/images/main.webp") center / cover no-repeat;
  transform: scale(1.015);
  filter: saturate(.9) contrast(1.03) brightness(.9);
}

/* =========================================================
   Noise
========================================================= */
.hero-noise {
  position: absolute;
  inset: 0;
  opacity: .045;
  background: url("data:image/svg+xml,%3Csvg viewBox='0 0 160 160' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.85' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='.5'/%3E%3C/svg%3E");
  mix-blend-mode: soft-light;
}

/* =========================================================
   Particles
========================================================= */
.hero-particles {
  position: absolute;
  inset: 0;
  opacity: .25;
}

.hero-particles span {
  position: absolute;
  width: 2px;
  height: 2px;
  background: rgba(220, 232, 218, .7);
  box-shadow: 0 0 8px rgba(190, 215, 190, .35);
  animation: particle-float 8s ease-in-out infinite;
}

/* 位置はそのまま */
.hero-particles span:nth-child(1) { top: 20%; left: 17%; animation-delay: -1s; }
.hero-particles span:nth-child(2) { top: 32%; left: 74%; animation-delay: -4s; }
.hero-particles span:nth-child(3) { top: 62%; left: 14%; animation-delay: -2s; }
.hero-particles span:nth-child(4) { top: 56%; left: 86%; animation-delay: -5s; }
.hero-particles span:nth-child(5) { top: 18%; left: 62%; animation-delay: -3s; }
.hero-particles span:nth-child(6) { top: 72%; left: 68%; animation-delay: -6s; }

/* =========================================================
   Content
========================================================= */
.hero-content {
  position: relative;
  z-index: 2;
  width: 100%;
  max-width: var(--content-width);
  margin: auto;
  padding: 0 var(--page-padding);
  display: flex;
  justify-content: center;
}

.hero-description {
  width: min(100%, 900px);
  margin: auto;
  text-align: center;
  font-size: 13px; /* ← 少し縮小 */
  font-weight: 500;
  line-height: 1.6; /* ← 縦幅に合わせて調整 */
}

.hero-description strong {
  display: inline-block;
  margin-bottom: 2px;
  font-size: 15px; /* ← 少し縮小 */
  font-weight: 800;
  line-height: 1.5;
}

/* =========================================================
   Light Mode（黒文字・影なし）
========================================================= */
@media (prefers-color-scheme: light) {
  .hero-description,
  .hero-description strong {
    color: #000;
    text-shadow: none;
  }
}

/* =========================================================
   Dark Mode（白文字・影なし）
========================================================= */
@media (prefers-color-scheme: dark) {
  .hero-description,
  .hero-description strong {
    color: #fff;
    text-shadow: none;
  }
}

/* =========================================================
   Animation
========================================================= */
@keyframes particle-float {
  0%, 100% { opacity: .15; transform: translateY(0); }
  50% { opacity: .6; transform: translateY(-8px); }
}

/* =========================================================
   Mobile
========================================================= */
@media (max-width: 768px) {
  .hero { height: 160px; } /* ← モバイルも縮小 */
  .hero-image { background-position: 58% center; }

  .hero-description {
    padding: 0 var(--page-padding);
    font-size: 12px;
    line-height: 1.55;
  }
  .hero-description strong {
    font-size: 13px;
    line-height: 1.45;
  }
  .desktop-break { display: none; }
}

@media (max-width: 480px) {
  .hero { height: 170px; }
  .hero-description { font-size: 11px; line-height: 1.5; }
  .hero-description strong { font-size: 12px; }
}
</style>
